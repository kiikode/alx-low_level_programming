#include <stdio.h>

/**
 * main - Print the number of arguments
 *
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
